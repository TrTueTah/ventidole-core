# ============================================
# Firebase Configuration for Mobile Apps
# ============================================

# Firebase Service Account (choose one method)
# --------------------------------------------

# Method 1: File path (recommended for local development)
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# Method 2: JSON string (recommended for production/Docker)
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'

# Firebase Project Settings
# --------------------------
# Get these from Firebase Console > Project Settings

# Database URL (Realtime Database - optional)
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com

# Storage Bucket (required for file uploads)
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# ============================================
# How to Get Firebase Credentials
# ============================================
#
# 1. Go to https://console.firebase.google.com
# 2. Select your project (or create new one)
# 3. Click the gear icon (⚙️) > Project Settings
# 4. Go to "Service Accounts" tab
# 5. Click "Generate New Private Key"
# 6. Save the downloaded JSON file as serviceAccountKey.json
# 7. Update the paths above
#
# ============================================

# ============================================
# PostgreSQL (existing - already configured)
# ============================================
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ============================================
# Other existing configuration...
# ============================================
