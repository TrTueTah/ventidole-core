/**
 * Token Strategy Keys
 */
export enum TokenStrategyKey {
  Jwt = 'jwt',
}

/**
 * Token Issuer Types
 */
export enum TokenIssuer {
  Access = 'access',
  Sensitive = 'sensitive',
  Refresh = 'refresh',
}
