export enum QueueName {
  Verification = "verification",
  Notification = "notification",
}

export enum JobName {
  SendMail = "send_mail",
  SendMultiNotifications = "send_multi_notifications",
  SendNotification = "send_notification",
}
