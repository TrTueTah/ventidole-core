export enum TokenStrategyKey {
  Jwt = "jwt",
}

export enum TokenIssuer {
  Access = "access",
  Sensitive = "sensitive",
  Refresh = "refresh",
}

export enum SocialLoginProvider {
  GOOGLE = "GOOGLE",
  FACEBOOK = "FACEBOOK",
}
