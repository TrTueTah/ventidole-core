version: '3.9'

services:
  server:
    build:
      context: ../..
      dockerfile: docker/prod/Dockerfile
    container_name: ventidole-server
    env_file:
      - ../../.env
    ports:
      - '8080:8080'
    restart: always
